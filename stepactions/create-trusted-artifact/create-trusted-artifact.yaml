---
apiVersion: tekton.dev/v1beta1
kind: StepAction
metadata:
  name: create-trusted-artifact
spec:
  description: >-
    This stepaction creates a trusted artifact. It does nothing if a .skip-trusted-artifacts file exists
    in root folder.
  image: quay.io/redhat-appstudio/build-trusted-artifacts@sha256:9de805309da402787ddb4ad350570ff03437ef513abb1e2256497fbeef306cee
  env:
    # Set HOME variable to something else than `/` to avoid 'permission denied' problems when writing files.
    - name: HOME
      value: /tekton/home
    - name: ORAS_OPTIONS
      value: $(params.orasOptions)
  params:
    - name: ociStorage
      type: string
      default: "empty"
    - name: workDir
      type: string
    - name: sourceDataArtifact
      type: string
      default: ""
    - name: orasOptions
      type: string
      default: ""
      description: oras options to pass to Trusted Artifacts calls
  args:
    - create
    - --store
    - $(params.ociStorage)
    - $(params.sourceDataArtifact)=$(params.workDir)
